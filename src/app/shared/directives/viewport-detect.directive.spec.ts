import { ViewportDetectDirective } from './viewport-detect.directive';

describe('ViewportDetectDirective', () => {
  it('should create an instance', () => {
    const directive = true;
    expect(directive).toBeTruthy();
  });
});
